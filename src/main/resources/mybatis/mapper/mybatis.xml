<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="profile_test">
    <select id="Mainprofile"  resultType="inus.main.profile.vo.profilelistVO">
        SELECT m_name, m_nickname, m_memo FROM MEMBERMANAGE_TBL
    </select>
    <select id="SelectMusicImport"  resultType="inus.main.profile.vo.profilelistVO">
        SELECT * FROM recomandList
    </select>
</mapper>